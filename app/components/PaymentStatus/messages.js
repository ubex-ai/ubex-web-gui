/*
 * PaymentStatus Messages
 *
 * This contains all the text for the PaymentStatus component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.PaymentStatus';

export default defineMessages({
	header: {
		id: `${scope}.header`,
		defaultMessage: 'This is the PaymentStatus component!',
	},
});
