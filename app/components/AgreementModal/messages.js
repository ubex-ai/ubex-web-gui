/*
 * AgreementModal Messages
 *
 * This contains all the text for the AgreementModal component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AgreementModal';

export default defineMessages({
	header: {
		id: `${scope}.header`,
		defaultMessage: 'This is the AgreementModal component!',
	},
});
