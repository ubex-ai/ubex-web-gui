/*
 * OpenIdBlock Messages
 *
 * This contains all the text for the OpenIdBlock component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.OpenIdBlock';

export default defineMessages({
	metamaskText: {
		id: `${scope}.metamaskText`,
		defaultMessage: 'You should previously install metamask browser plugin.',
	},
});
