/*
 * RestorePassword Messages
 *
 * This contains all the text for the RestorePassword component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RestorePassword';

export default defineMessages({
	header: {
		id: `${scope}.header`,
		defaultMessage: 'Restore password',
	},
});
