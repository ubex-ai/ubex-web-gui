/*
 * SitesTable Messages
 *
 * This contains all the text for the SitesTable component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SitesTable';

export default defineMessages({
	header: {
		id: `${scope}.header`,
		defaultMessage: 'This is the SitesTable component!',
	},
});
